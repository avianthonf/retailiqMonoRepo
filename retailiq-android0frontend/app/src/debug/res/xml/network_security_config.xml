<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug builds need to reach emulator, loopback, and LAN-hosted backends over HTTP. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
